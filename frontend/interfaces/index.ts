export type SampleData = {
  id: string;
  picture: string;
  birthday: string;
  name: string;
  address: string;
  phone_number: string;
};
export type AvatarInterface = {
  avatarSize: number;
  image: string;
};
